#include<stdio.h>
void main()
{
    int a,b,count;
    printf("Enter two number:");
    scanf("%d%d",&a,&b);
    for(int i=a;i<=b;i++)
    { 
        count=0;
        for(int j=2;j<=a;j++)
        {
            if(i%j==0)
            {
                count+=1;
            }
        }
        if(count==0)
        {
            printf("  %d",i);
        }
    }    
}
